query ProductCategoryBySlug($slug: String) {
	category(where: { slug: $slug }) {
		name
		description
		products {
			name
			id
			images {
				url
			}
			price
			categories {
				name
			}
			rating
		}
	}
}
