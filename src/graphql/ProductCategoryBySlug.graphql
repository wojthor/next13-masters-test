query ProductCategoryBySlug($slug: String) {
	category(where: { slug: $slug }) {
		name
		description
		products(first: 500) {
			name
			id
			images {
				url
			}
			price
			categories {
				name
			}
			rating
		}
	}
}
